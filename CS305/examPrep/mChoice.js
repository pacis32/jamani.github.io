/* eslint-disable require-jsdoc */
/* eslint-disable strict */
const numArray=[1,2,3,4,44];
//use array.map to get new array with all the values of numArray doubled

const newArray= numArray.map(item=> item*2){
    return item;
}