/* eslint-disable strict */
// [1,5,16,3, 108]

[1,5,16,3, 108].forEach(item=> {if (item%2==0)
    console.log(item);});

    const numbers = [1, 5, 18, 2, 77, 108];

    function isEven(number){
      if (number %2==0){
        return true;
      }
      else
      return false;
        }
    }

    numbers.filter(isEven);

    numbers.findIndex(isEven,0);
       
